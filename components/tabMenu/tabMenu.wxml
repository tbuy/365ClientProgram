  <view class='menu'>
    <view wx:for="{{menuList}}" wx:key="{{item.id}}" class='menu-item {{item.isHeightlight?"menu-item-active":""}}' bindtap='selected' data-id="{{item.id}}">{{item.title}}</view>
  </view>