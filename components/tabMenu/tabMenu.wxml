  <view class='menu'>
    <view wx:for="{{list}}" wx:key="{{item.id}}" class='menu-item {{item.isHeightlight?"menu-item-active":""}}' bindtap="selected" data-id="{{item.id}}" style="width: {{width}}">{{item.title}}</view>
  </view>